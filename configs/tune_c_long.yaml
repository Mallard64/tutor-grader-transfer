# Arm C: arm A run for 8 epochs instead of 4.
# Validation was still climbing at epoch 4 on at least one seed, so 4 may
name: tune_c_long
model_name: microsoft/deberta-v3-base
train_domain: math
eval_domain: math
k_programming: 0
seed: 0
max_length: 512
learning_rate: 2.0e-5
batch_size: 16
eval_batch_size: 32
num_epochs: 8.0
warmup_ratio: 0.1
weight_decay: 0.01
gradient_accumulation_steps: 1
class_weights: false
init_from: ""
output_dir: models/runs

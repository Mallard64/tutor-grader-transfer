# Arm A: the current recipe, plus best-checkpoint selection on validation.
# This is the control -- B and C differ from it in exactly one field each.
name: tune_a_base
model_name: microsoft/deberta-v3-base
train_domain: math
eval_domain: math
k_programming: 0
seed: 0
max_length: 512
learning_rate: 2.0e-5
batch_size: 16
eval_batch_size: 32
num_epochs: 4.0
warmup_ratio: 0.1
weight_decay: 0.01
gradient_accumulation_steps: 1
class_weights: false
init_from: ""
output_dir: models/runs

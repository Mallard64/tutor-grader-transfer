# Only needed for scripts/build_candidates.py (response generation)
# and experiments/b_llm_judge.py (zero-shot judge). Not needed for
# the DeBERTa experiments (c, d, e) or the tests.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Optional: any OpenAI-compatible endpoint (Together, Fireworks, vLLM, ...)
OPENAI_COMPAT_BASE_URL=https://api.together.xyz/v1
OPENAI_COMPAT_API_KEY=...
